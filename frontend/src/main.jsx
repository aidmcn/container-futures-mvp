import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
// If global styles are needed, they can be imported here, e.g.:
// import './index.css'; // Or any other global stylesheet

ReactDOM.createRoot(document.getElementById('app')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
); 